!!!
%html
  %head
    %title Package Tracker
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults, 'jquery', 'jquery-ui', 'rails'
    = csrf_meta_tag
    = yield(:head)
  %body
    #content
      = yield
      - flash.each do |name, msg|
        = content_tag :div, msg, id: "flash_#{name}"
